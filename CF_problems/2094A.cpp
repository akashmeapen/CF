#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        string s, s1, s2;
        cin >> s >> s1 >> s2;
        cout << s[0] << s1[0] << s2[0] << endl;
    }
}